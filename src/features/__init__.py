"""Phase 4 — feature engineering.

One transform per module (`add_<x>_features(df) -> df`), side-effect-free, NaN-safe, no
imputation, no row drops. `build_features.build_features` orchestrates them in order.
"""
